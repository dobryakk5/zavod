project-root/
│
├── backend/              ← Wagtail + Django + DRF + Celery
│   ├── manage.py
│   ├── backend/          ← Django settings
│   ├── core/             ← твои модели и бизнес-логика multi-tenant
│   ├── posts/            ← можно отдельные apps
│   ├── schedule/
│   ├── social/
│   ├── templates/
│   └── ...
│
├── frontend/             ← Next.js (личные кабинеты клиентов)
│   ├── pages/
│   ├── components/
│   ├── libs/api/
│   └── ...
│
├── ai-worker/            ← отдельный Python-сервис (опционально)
│   ├── tasks/
│   ├── openai/
│   ├── stability/
│   ├── video/
│   └── ...
│
├── docker/               ← docker-compose, Dockerfile, env
│
└── docs/                 ← документация, схемы, диаграммы


То есть backend — это Django + Wagtail, в котором:
живут модели (Client, Post, Schedule, SocialAccount),
REST/GraphQL API (DRF),
Celery задачи,
логика мультиарендности,
доступ к базе,
CMS-админка.

Вот **чёткий и реалистичный план внедрения** системы multi-tenant контент-платформы (Python + Wagtail + Celery + Next.js), разбитый на этапы, чтобы можно было стартовать быстро и масштабировать без переделок.

Я сделаю план в 4 уровнях:
**MVP → Production-ready → Масштабирование → Автоматизация AI и соцсетей.**

---

# 🚀 **План внедрения**

---

# **ЭТАП 1 — База платформы (7–10 дней)**

### 🎯 Цель: запустить каркас multi-tenant + CMS для ручного контента.

## 1.1. **Структура проектов**

Создать 3 репозитория/директории:

* `backend/` — Django + Wagtail + DRF + Celery
* `frontend/` — Next.js личные кабинеты
* `ai-worker/` — Python сервис (если потребуется вынести тяжёлые AI-задачи)

## 1.2. **Поднять Django + Wagtail**

* создать Django-проект
* подключить Wagtail (`wagtail start`)
* PostgreSQL как база данных
* создать базовый admin-superuser

## 1.3. **Сделать модель арендатора (Client)**

```python
class Client(models.Model):
    name = models.CharField()
    slug = models.SlugField(unique=True)
    timezone = models.CharField()
```

## 1.4. **Мульти-тенантность пользователей**

* `UserTenantRole` модель (user → client → role)
* middleware, который определяет активный client
* выбор клиента после входа (если их несколько)

## 1.5. **Модели контента**

Создать модели:

* `Post`
* `Media` (или использовать Wagtail Documents/Images)
* `Schedule`
* все с FK на `Client`.

## 1.6. **Интерфейс администратора**

* зарегистрировать модели в Wagtail ModelAdmin
* добавить фильтры по client
* запретить доступ к чужим данным (переписать `get_queryset`)

### ✔ Результат этапа:

* Multi-tenant CMS работает
* Каждый клиент видит только свои посты
* Можно вручную создавать посты и расписания

---

# **ЭТАП 2 — Backend API для личных кабинетов (3–5 дней)**

### 🎯 Цель: дать клиентам личный кабинет на Next.js

## 2.1. Поднять DRF API (или GraphQL)

Эндпоинты:

* `GET /posts/` — список постов арендатора
* `POST /posts/` — создать
* `PATCH /posts/id/` — обновить
* `GET /schedule/` — расписание
* `POST /schedule/` — добавление публикации

Все запросы автоматически фильтруются по активному `client`.

## 2.2. Авторизация

Лучший вариант:

* Django + SimpleJWT
* Next.js хранит refresh токен httpOnly cookie
* Access Token в памяти

## 2.3. Next.js кабинет

Минимальный функционал:

* Логин → выбор клиента
* Список постов
* Страница редактирования поста
* Календарь расписаний
* Кнопка «Запланировать публикацию»

### ✔ Результат этапа:

* Клиенты могут сами создавать и утверждать контент
* Есть безопасный доступ
* Есть полноценный кабинет арендатора

---

# **ЭТАП 3 — AI модуль (7–14 дней)**

### 🎯 Цель: автоматическая генерация контента — текст ⟶ картинка ⟶ видео.

## 3.1. Добавить Celery

* celery worker
* celery beat (cron)

## 3.2. Скрейпинг и поиск вдохновляющего контента

Таски:

* RSS
* NewsAPI
* YouTube Data search
* Google Trends

Каждый таск знает `client_id`.

## 3.3. Генерация текста

* OpenAI GPT-4
* Настраиваем tonе-of-voice на уровне `Client`:

  * стиль
  * длина
  * формат

## 3.4. Генерация изображений

* Stable Diffusion API (Stability AI)

## 3.5. Генерация видео (если нужно)

* RunwayML API → async job
* Celery периодически проверяет status

## 3.6. Интеграция с Post моделью

После генерации создать `Post(client=..., status='draft', ...)`.

### ✔ Результат этапа:

* По расписанию система сама делает посты для каждого клиента
* Редакторы клиентов видят их как черновики

---

# **ЭТАП 4 — Автоматическая публикация в соцсети (5–10 дней)**

### 🎯 Цель: полностью автоматизировать постинг.

## 4.1. Модель SocialAccount

С полями:

* platform
* token
* refresh_token
* channel/page ID

## 4.2. Подключения:

* Telegram Bot API
* Instagram Graph API
* YouTube Data API

В каждой Celery задаче:

* берём `Account`
* берём `Post`
* постим
* сохраняем external_id

## 4.3. Расписание

Celery beat каждые N минут:

```
SELECT * FROM schedule
WHERE scheduled_at <= now() 
AND status='pending'
```

Потом вызывает `publish(schedule_id)`.

## 4.4. Логи и ретраи

* при ошибке статус → `failed`
* retry через 10 минут
* лог в поле `Schedule.log`

### ✔ Результат этапа:

* Контент генерируется автоматически
* Публикуется автоматически
* Всё по каждому клиенту строго изолировано

---

# **ЭТАП 5 — Продакшн, масштабирование, безопасность (5–10 дней)**

### 🎯 Цель: система должна выдерживать рост среди клиентов

## 5.1. Развёртка

* Docker Compose → Docker → Kubernetes (если нужно)
* Отдельные сервисы:

  * backend
  * frontend
  * celery worker
  * celery beat
  * redis
  * postgres

## 5.2. Полная изоляция клиентов

* row-level permission в API
* фильтры в админке
* раздельные медиахранилища (папка `/client_slug/`)

## 5.3. Мониторинг

* Sentry
* Prometheus/Grafana
* Healthcheck endpoints

---

# **Итог — что будет после реализации**

Ты получишь платформу уровня **SaaS-контент-студии**:

* мульти-тенантная архитектура
* автоматический сбор информации
* автоматическая генерация текста, фото, видео
* удобный кабинет для клиентов
* AI-помощник для подготовки контента
* авто-постинг во все соцсети
* логирование, статистика, KPI


