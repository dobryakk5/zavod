import { QuizBuilder } from '@/components/quiz/quiz-builder';

export const metadata = {
  title: 'Конструктор квизов',
};

export default function ClientQuizBuilderPage() {
  return <QuizBuilder />;
}
