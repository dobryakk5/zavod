# AI Content Generator - Инструкция по настройке и использованию

## 1. Установка зависимостей

```bash
cd /Users/pavellebedev/Desktop/proj/zavod
. venv/bin/activate
pip install -r backend/requirements.txt
```

## 2. Получение OpenRouter API ключа

1. Зарегистрируйтесь на https://openrouter.ai/
2. Получите API ключ: https://openrouter.ai/keys
3. Добавьте ключ в `backend/.env`:

```bash
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-здесь
```

**Важно**: Модель `deepseek/deepseek-chat-v3.1:free` бесплатная, но требует регистрации.

## 3. Запуск системы

### Terminal 1: Django сервер
```bash
cd /Users/pavellebedev/Desktop/proj/zavod
. venv/bin/activate
cd backend
python manage.py runserver
```

### Terminal 2: Celery worker
```bash
cd /Users/pavellebedev/Desktop/proj/zavod
. venv/bin/activate
cd backend
celery -A config worker -l info
```

## 4. Создание Content Template (шаблона контента)

Откройте Django admin: http://localhost:8000/django-admin/

### Создайте шаблон контента:
1. Перейдите в **Content templates**
2. Нажмите **Add Content Template**

### Заполните поля:

**Базовые:**
- **Client**: выберите клиента (создайте если нет)
- **Name**: "Instagram пост - дружественный"
- **Is default**: ✓ (отметьте для первого шаблона)

**Стиль:**
- **Tone**: Friendly (Дружественный)
- **Length**: Medium (Средний)
- **Language**: Russian

**Prompt template** (скопируйте и вставьте):
```
Ты - опытный SMM-менеджер для "{topic_name}".

Создай {length} пост в {tone} стиле на {language} языке.

НОВОСТЬ/ТРЕНД:
Заголовок: {trend_title}
Описание: {trend_description}

ЗАДАЧА:
1. Напиши привлекательный заголовок (до 100 символов)
2. Создай основной текст который:
   - Объясняет суть новости простым языком
   - Показывает практическую пользу для аудитории
   - Связан с темой "{topic_name}"
   - Написан в {tone} тоне
   - Имеет длину: {length}
3. Добавь эмодзи для оживления текста
4. Закончи призывом к действию

Будь креативным но информативным!
```

**Additional instructions** (опционально):
```
Всегда упоминай бренд в конце поста.
Используй emoji умеренно (2-3 в тексте).
Избегай сложных терминов.
```

**Хэштеги:**
- **Include hashtags**: ✓
- **Max hashtags**: 5

## 5. Использование

### Шаг 1: Найти тренды
1. Перейдите в **Topics**
2. Выберите тему (или создайте новую)
3. Выберите action: **"Найти тренды для выбранных тем"**
4. Нажмите **Go**

### Шаг 2: Сгенерировать посты
После того как тренды найдены (проверьте в **Trend Items**):

**Вариант А: Из трендов**
1. Перейдите в **Trend Items**
2. Выберите тренды (те, что не использованы)
3. Выберите action: **"Сгенерировать посты из выбранных трендов"**
4. Нажмите **Go**

**Вариант Б: Из темы**
1. Перейдите в **Topics**
2. Выберите тему
3. Выберите action: **"Сгенерировать посты из неиспользованных трендов"**
4. Нажмите **Go**

### Шаг 3: Проверить результаты
1. Перейдите в **Posts**
2. Вы увидите новые посты со статусом **Draft**
3. Откройте пост для редактирования
4. При необходимости отредактируйте текст
5. Измените статус на **Ready** или **Approved**

## 6. Структура сгенерированного поста

```
Title: Привлекательный заголовок
Status: draft (требует модерации)
Text: Полный текст поста с хэштегами
Tags: ["хэштег1", "хэштег2", "ai_generated", "source_google_news_rss"]
Source links: [ссылка на оригинальную новость]
Generated by: openrouter-deepseek
```

## 7. Примеры промптов для разных стилей

### Профессиональный (Business)
```
Ты - эксперт в области {topic_name}.

Создай {length} профессиональный обзор на {language} языке.

ИСТОЧНИК: {trend_title}
{trend_description}

Напиши экспертный анализ:
- Объективная оценка новости
- Влияние на индустрию
- Практические выводы
- Профессиональный tone of voice
```

### Восторженный (Enthusiastic)
```
Ты - энтузиаст {topic_name}, который делится крутыми новостями!

🔥 ГОРЯЧАЯ НОВОСТЬ: {trend_title}

Создай {length} пост который ВДОХНОВЛЯЕТ:
- Начни с wow-эффекта
- Покажи почему это КРУТО
- Используй много энергии и эмодзи
- Заверши мотивирующим CTA

Пиши на {language} языке в {tone} стиле!
```

## 8. Мониторинг и логи

### Проверить задачи Celery:
В терминале с Celery worker вы увидите:
```
[2025-01-26 12:00:00] INFO: Генерация поста из тренда: современные танцы...
[2025-01-26 12:00:05] INFO: Используется шаблон: Instagram пост - дружественный
[2025-01-26 12:00:10] INFO: Успешно создан пост ID=42 из тренда ID=15
```

### Типичные ошибки:

**"OPENROUTER_API_KEY not found"**
- Проверьте файл `backend/.env`
- Убедитесь что ключ раскомментирован
- Перезапустите Celery worker

**"Нет шаблонов контента для клиента"**
- Создайте ContentTemplate для клиента
- Убедитесь что is_default=True

**"Тренд уже использован"**
- Это нормально - тренд может использоваться только один раз
- Найдите новые тренды или выберите неиспользованные

## 9. Тестирование генератора

Запустите Django shell для тестирования:

```bash
cd backend
python manage.py shell
```

```python
from core.ai_generator import AIContentGenerator
from core.models import Client, ContentTemplate, Topic, TrendItem

# Тест подключения к API
generator = AIContentGenerator()
print(generator.test_connection())  # Должно вернуть True

# Тест генерации
result = generator.generate_post_text(
    trend_title="Новый стиль танца стал вирусным в TikTok",
    trend_description="Миллионы пользователей повторяют новый танец",
    topic_name="студия танцев",
    template_config={
        'tone': 'friendly',
        'length': 'medium',
        'language': 'ru',
        'prompt_template': '',  # использует дефолтный
        'additional_instructions': '',
        'include_hashtags': True,
        'max_hashtags': 5
    }
)

print(result)
```

## 10. Расширенные настройки

### Изменить AI модель
В `backend/core/ai_generator.py`:
```python
self.model = "deepseek/deepseek-chat-v3.1:free"  # бесплатная
# или
self.model = "anthropic/claude-3.5-sonnet"  # платная, более качественная
```

### Настроить temperature (креативность)
В `backend/core/tasks.py`, функция `generate_post_from_trend`:
```python
result = generator.generate_post_text(
    ...
    temperature=0.9  # 0.0 = очень предсказуемо, 1.0 = очень креативно
)
```

### Создать несколько шаблонов для одного клиента
- Instagram: короткий, дружественный, эмодзи
- LinkedIn: длинный, профессиональный, без эмодзи
- Telegram: средний, информационный, быстро читается

## 11. Troubleshooting

**Посты не генерируются:**
1. Проверьте Celery worker запущен
2. Проверьте логи Celery
3. Убедитесь что API ключ валидный
4. Проверьте что тренды не использованы

**Качество постов низкое:**
1. Улучшите prompt_template
2. Добавьте additional_instructions
3. Попробуйте другую модель
4. Измените temperature

**Хэштеги на английском вместо русского:**
- Укажите в промпте: "Хэштеги должны быть на русском языке"
- Добавьте примеры: #танцы #хореография

## Готово! 🎉

Теперь у вас работает автоматическая генерация контента с AI!
